/*
|--------------------------------------------------------------------------
| Endpoints - Service Calls
|--------------------------------------------------------------------------
*/

require('dotenv').config();

module.exports = {
    RICK_MORTY_ENDPOINT: process.env.RICK_MORTY_ENDPOINT,
    LARAVEL_ASSIGNMENT_ENDPOINT: process.env.LARAVEL_ASSIGNMENT_ENDPOINT,
};